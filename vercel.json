{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "installCommand": "npm install --legacy-peer-deps && npm install -D autoprefixer postcss tailwindcss",
          "buildCommand": "npm run build"
        }
      }
    ]
  }